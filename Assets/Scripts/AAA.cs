using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AAA : MonoBehaviour {
    
    // Use this for initialization
    void Start () {
        // //   Application.targetFrameRate = 60;
        // int item_amount = 100;
     // PlayerPrefs.DeleteAll();
        // PlayerPrefs.SetInt("Rocket" + "amount", item_amount);
        // PlayerPrefs.SetInt("Shield" + "amount", item_amount);
        // PlayerPrefs.SetInt("DoubleCoin" + "amount", item_amount);
        // PlayerPrefs.SetInt("Magnet" + "amount", item_amount);
        // PlayerPrefs.SetInt("Timer" + "amount", item_amount);
        

    }

    // Update is called once per frame
    
}
