using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Determinate_sequence<type> : MonoBehaviour//Sequence<type>
{
    /*public List<type> elements;

    private int current = 0;

    public override type get_next()
    {
        if (current >= elements.Count)
            return default(type);
        else
        {
            current++;
            return elements[current - 1];
        }
    }*/
}
